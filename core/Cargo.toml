[package]
name = "vault_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "vault_core"

[dependencies]
zeroize = { version = "1", features = ["derive"] }
argon2 = "0.5"
password-hash = {version = "0.5" , features = ["getrandom"]}


